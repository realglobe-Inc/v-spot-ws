/**
 * Test for VSpotWSClient.
 * Runs with mocha.
 */
'use strict'

const VSpotWSClient = require('../lib/VSpotWSClient')
const {ok, equal} = require('assert')

describe('v-spot-w-s-client', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
