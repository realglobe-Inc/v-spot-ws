/**
 * Test for VSpotWSServer.
 * Runs with mocha.
 */
'use strict'

const VSpotWSServer = require('../lib/VSpotWSServer')
const { ok, equal } = require('assert')

describe('v-spot-w-s-server', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
