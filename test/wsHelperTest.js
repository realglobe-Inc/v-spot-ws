/**
 * Test for wsHelper.
 * Runs with mocha.
 */
'use strict'

const wsHelper = require('../lib/helpers/wsHelper')
const { ok, equal } = require('assert')

describe('ws-helper', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
