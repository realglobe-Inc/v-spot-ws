/**
 * Test for VSpotWS.
 * Runs with mocha.
 */
'use strict'

const VSpotWS = require('../lib/VSpotWS')
const { ok, equal } = require('assert')

describe('v-spot-w-s', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
